python-telegram-bot[job-queue]==21.6
# yt-dlp must stay current — YouTube breaks older versions frequently.
# [default] pulls the EJS challenge-solver scripts needed (with a JS runtime
# like Deno) to solve YouTube's n-challenge. See the EJS wiki page.
yt-dlp[default]>=2025.1.1
